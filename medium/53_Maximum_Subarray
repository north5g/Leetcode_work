class Solution:
    def maxSubArray(self, nums: List[int]) -> int:
        # running sum
        # while current > 0, better to add to max

        max = float("-inf")
        current = 0

        for pointer in nums:
            current += pointer
            if current > max:
                max = current
            if current < 0:
                current = 0
        
        return max