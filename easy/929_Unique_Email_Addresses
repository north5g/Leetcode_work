class Solution:
    def numUniqueEmails(self, emails: List[str]) -> int:
        emailList = set()
        for i in emails:
            local, domain = i.split('@')
            local = local.split('+')[0]
            local = local.replace(".", "")
            emailList.add(local + "@" + domain)
        return len(emailList)